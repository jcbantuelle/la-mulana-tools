<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">La-Mulana</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Save File Editor', save_file_edit_path, class: "nav-link #{active_class('save_file')}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Reference Guide', reference_path, class: "nav-link #{active_class('reference')}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Multiworld', multiworlds_path, class: "nav-link #{active_class('multiworlds')}" %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Friends', friends_path, class: "nav-link #{active_class('friends')}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'nav-link active btn btn-sm btn-primary' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log In', new_user_session_path, class: 'nav-link active btn btn-sm btn-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
