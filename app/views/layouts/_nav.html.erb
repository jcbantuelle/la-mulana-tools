<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">La-Mulana</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link <%= 'active' if request.fullpath.include?('/save_file/') %>" href="/save_file/edit">Save File Editor</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if request.fullpath == '/reference' %>" href="/reference">Reference Guide</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if request.fullpath == '/multiworlds' %>" href="/multiworlds">Multiworld</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link <%= 'active' if request.fullpath == '/friends' %>" href="/friends">Friends</a>
          </li>
          <li class="nav-item">
            <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'nav-link active btn btn-sm btn-primary' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log In', new_user_session_path, class: 'nav-link active btn btn-sm btn-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
